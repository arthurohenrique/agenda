# Secrets Exposure Fix Plan

- [x] Ignorar `.env*` e manter `.env.example` com placeholders.
- [x] Impedir segredo em `NEXT_PUBLIC_*`.
- [ ] Proprietário rotaciona segredo exposto e remove/troca contas demo remotas.
- [ ] Executar scanner de histórico antes do lançamento.
